#include<iostream>
using namespace std;
int main() {
    /*MenOfPassion(A[], n) {
        sum < -0;
        for i < -1 to n
            sum < -sum + A[i]; # 코드1
            return sum;
    }*/
    //시간 복잡도 O(n)
    int n;
    cin >> n;
    cout << n<<"\n"<< 1;
    return 0;
}